# packages/api-client — lands in the auth sprint

Generated TypeScript types + client from the FastAPI OpenAPI schema
(single source of truth; CI fails on drift). Until then, apps/web keeps a
small hand-written mirror in src/lib/types.ts.
